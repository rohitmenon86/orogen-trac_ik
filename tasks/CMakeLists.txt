# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(trac_ikTaskLib)
ADD_LIBRARY(${TRAC_IK_TASKLIB_NAME} SHARED 
    ${TRAC_IK_TASKLIB_SOURCES}
    ${Boost_SYSTEM_LIBRARY}
    ${Boost_FILESYSTEM_LIBRARY}
    ${Boost_PROGRAM_OPTIONS_LIBRARY}
    ${Boost_THREAD_LIBRARY}
    ${Boost_MULTIPRECISION_LIBRARY})
add_dependencies(${TRAC_IK_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${TRAC_IK_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TRAC_IK_TASKLIB_DEPENDENT_LIBRARIES}
    ${Boost_SYSTEM_LIBRARY}
    ${Boost_FILESYSTEM_LIBRARY}
    ${Boost_PROGRAM_OPTIONS_LIBRARY}
    ${Boost_THREAD_LIBRARY}
    ${Boost_MULTIPRECISION_LIBRARY})
SET_TARGET_PROPERTIES(${TRAC_IK_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TRAC_IK_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${TRAC_IK_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${TRAC_IK_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TRAC_IK_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TRAC_IK_TASKLIB_HEADERS}
    DESTINATION include/orocos/trac_ik)
set(CMAKE_CXX_FLAGS "-std=c++11 ${CMAKE_CXX_FLAGS}")
